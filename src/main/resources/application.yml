spring:
#  profiles:
#    active: dev   # Default profile unless overridden
  datasource:
    url: jdbc:mysql://localhost:3306/bank_application?createDatabaseIfNotExist=true
    username: root
    password: 12345
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update #create-drop


bootstrap:
  admin:
    username: admin
    password: admin123

    # Optional: log SQL queries
#  show-sql: true
#  properties:
#    hibernate:
#      format_sql: true